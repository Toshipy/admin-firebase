import React, { useEffect } from 'react';
import { useSelector, useDispatch } from 'react-redux';
import { useUser, login, logout } from './features/userSlice'
import { auth } from './firebase';
import './App.css';
import styles from "./App.module.css";

function App() {
  return (
    <div className="App">
      
    </div>
  );
}

export default App;
